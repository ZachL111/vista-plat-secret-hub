# Golden Cases

The golden set keeps `rollout width` and `route drift` visible as named cases.

The main golden fixture is `fixtures/golden/scoreboard.csv`. The matrix fixture is `fixtures/golden/lane-matrix.csv`. Together they cover `rollout width`, `quota pressure`, `route drift`, and `secret scope` with different score ranges.

The purpose is to make intentional rule changes show up in review.
